import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//We put inside curly brackets because it's one of many elements form Context.js
ReactDOM.render(<App />, document.getElementById('root'));
